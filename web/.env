VITE_API_URL=

# In production, the Docker build sets VITE_API_URL to the backend URL.
# For local dev, leave this blank so the app uses relative /users paths with Vite proxy.

# Example for production:
# VITE_API_URL=http://3.27.9.59:3000
